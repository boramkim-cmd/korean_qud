# 04. 검증된 오픈소스 코드 분석 및 최적화

**작성일시:** 2026-01-13 09:58  
**목적:** 실제 검증된 코드 분석 및 Caves of Qud 최적화

---

## 📚 분석 대상 라이브러리

### 1. Josa.js (JavaScript)
- **GitHub:** https://github.com/e-/Josa.js
- **Stars:** 수백 개
- **검증도:** ⭐⭐⭐⭐⭐ 매우 높음
- **코드 크기:** 약 60줄 (매우 간결)

---

## 🔍 Josa.js 핵심 코드 분석

### 원본 코드 (JavaScript)

```javascript
(function(){
  var _f = [
    function(string) { //을/를 구분
      return _hasJong(string) ? '을' : '를';
    },
    function(string){ //은/는 구분
      return _hasJong(string) ? '은' : '는';
    },
    function(string){ //이/가 구분
      return _hasJong(string) ? '이' : '가';
    },
    function(string){ //와/과 구분
      return _hasJong(string) ? '과' : '와';
    },
    function(string){ //으로/로 구분
      return _hasJong(string) ? '으로' : '로';
    }
  ],
    _formats = {
      '을/를' : _f[0],
      '을' : _f[0],
      '를' : _f[0],
      '을를' : _f[0],
      '은/는' : _f[1],
      '은' : _f[1],
      '는' : _f[1],
      '은는' : _f[1],
      '이/가' : _f[2],
      '이' : _f[2],
      '가' : _f[2],
      '이가' : _f[2],
      '와/과' : _f[3],
      '와' : _f[3],
      '과' : _f[3],
      '와과' : _f[3],
      '으로/로' : _f[4],
      '으로' : _f[4],
      '로' : _f[4],
      '으로로' : _f[4]
    };

  function _hasJong(string){ //string의 마지막 글자가 받침을 가지는지 확인
    string = string.charCodeAt(string.length - 1);
    return (string - 0xac00) % 28 > 0;
  }

  var josa = {
    c: function(word, format){
      if (typeof _formats[format] === 'undefined') throw 'Invalid format!';
      return _formats[format](word);
    },
    r: function(word, format) {
      return word + josa.c(word, format);
    }
  };

  // Export
  if (typeof define == 'function' && define.amd) {
    define(function(){ return josa; });
  } else if (typeof module !== 'undefined') {
    module.exports = josa;
  } else {
    window.Josa = josa;
  }
})();
```

---

## 💡 핵심 알고리즘 분석

### 1. 받침 감지 (_hasJong)

```javascript
function _hasJong(string){
  string = string.charCodeAt(string.length - 1);
  return (string - 0xac00) % 28 > 0;
}
```

**분석:**
- ✅ **매우 간결함** (2줄)
- ✅ 마지막 글자의 유니코드 값 추출
- ✅ `(코드 - 0xac00) % 28 > 0` → 받침 있음
- ⚠️ **한글 범위 체크 없음** (개선 필요)

**개선 사항:**
```javascript
function _hasJong(string){
  if (!string) return false;
  var code = string.charCodeAt(string.length - 1);
  // 한글 범위 체크 추가
  if (code < 0xac00 || code > 0xd7a3) return false;
  return (code - 0xac00) % 28 > 0;
}
```

### 2. 조사 선택 함수 배열

```javascript
var _f = [
  function(string) { return _hasJong(string) ? '을' : '를'; },
  function(string) { return _hasJong(string) ? '은' : '는'; },
  function(string) { return _hasJong(string) ? '이' : '가'; },
  function(string) { return _hasJong(string) ? '과' : '와'; },
  function(string) { return _hasJong(string) ? '으로' : '로'; }
];
```

**분석:**
- ✅ **함수 배열 패턴** - 깔끔함
- ✅ 각 조사 타입별 함수 분리
- ⚠️ **으로/로 특수 케이스 미처리** (ㄹ 받침)

**개선 사항:**
```javascript
function(string) { 
  // ㄹ 받침 특수 처리
  var code = string.charCodeAt(string.length - 1);
  if ((code - 0xac00) % 28 === 8) return '로'; // ㄹ 받침
  return _hasJong(string) ? '으로' : '로';
}
```

### 3. 포맷 맵핑

```javascript
_formats = {
  '을/를' : _f[0],
  '을' : _f[0],
  '를' : _f[0],
  '을를' : _f[0],
  // ...
};
```

**분석:**
- ✅ **유연한 입력 지원** - 여러 형식 허용
- ✅ 사용자 편의성 높음
- ✅ `"을/를"`, `"을"`, `"를"`, `"을를"` 모두 같은 함수

### 4. API 디자인

```javascript
josa.c(word, format)  // 조사만 반환
josa.r(word, format)  // 단어 + 조사 반환
```

**분석:**
- ✅ **2가지 API** - 선택권 제공
- ✅ `c` = choice (선택)
- ✅ `r` = result (결과)

---

## 🎯 C#으로 최적화 포팅

### 버전 1: 직역 (Josa.js 구조 유지)

```csharp
using System;
using System.Collections.Generic;

namespace CavesOfQud.KoreanJosa
{
    public static class Josa
    {
        // 조사 선택 함수 타입
        private delegate string JosaSelector(string word);
        
        // 조사 선택 함수 배열
        private static readonly JosaSelector[] Selectors = new JosaSelector[]
        {
            word => HasJongseong(word) ? "을" : "를",  // 0: 을/를
            word => HasJongseong(word) ? "은" : "는",  // 1: 은/는
            word => HasJongseong(word) ? "이" : "가",  // 2: 이/가
            word => HasJongseong(word) ? "과" : "와",  // 3: 와/과
            word => HasRieulJongseong(word) ? "로" :   // 4: 으로/로 (ㄹ 받침 처리)
                    HasJongseong(word) ? "으로" : "로"
        };
        
        // 포맷 맵핑
        private static readonly Dictionary<string, int> FormatMap = new Dictionary<string, int>
        {
            { "을/를", 0 }, { "을", 0 }, { "를", 0 }, { "을를", 0 },
            { "은/는", 1 }, { "은", 1 }, { "는", 1 }, { "은는", 1 },
            { "이/가", 2 }, { "이", 2 }, { "가", 2 }, { "이가", 2 },
            { "와/과", 3 }, { "와", 3 }, { "과", 3 }, { "와과", 3 },
            { "으로/로", 4 }, { "으로", 4 }, { "로", 4 }, { "으로로", 4 }
        };
        
        /// <summary>
        /// 받침 확인 (개선 버전)
        /// </summary>
        private static bool HasJongseong(string word)
        {
            if (string.IsNullOrEmpty(word))
                return false;
            
            char lastChar = word[word.Length - 1];
            
            // 한글 범위 체크
            if (lastChar < 0xAC00 || lastChar > 0xD7A3)
                return false;
            
            return (lastChar - 0xAC00) % 28 > 0;
        }
        
        /// <summary>
        /// ㄹ 받침 확인 (추가 기능)
        /// </summary>
        private static bool HasRieulJongseong(string word)
        {
            if (string.IsNullOrEmpty(word))
                return false;
            
            char lastChar = word[word.Length - 1];
            
            if (lastChar < 0xAC00 || lastChar > 0xD7A3)
                return false;
            
            return (lastChar - 0xAC00) % 28 == 8; // ㄹ = 8
        }
        
        /// <summary>
        /// 조사만 선택 (josa.c와 동일)
        /// </summary>
        public static string Choose(string word, string format)
        {
            if (!FormatMap.ContainsKey(format))
                throw new ArgumentException($"Invalid format: {format}");
            
            int index = FormatMap[format];
            return Selectors[index](word);
        }
        
        /// <summary>
        /// 단어 + 조사 반환 (josa.r과 동일)
        /// </summary>
        public static string Result(string word, string format)
        {
            return word + Choose(word, format);
        }
    }
}
```

### 사용 예시

```csharp
// 조사만 선택
string josa1 = Josa.Choose("검", "을/를");     // "을"
string josa2 = Josa.Choose("사과", "을/를");   // "를"

// 단어 + 조사
string result1 = Josa.Result("검", "을/를");   // "검을"
string result2 = Josa.Result("사과", "을/를"); // "사과를"

// 다양한 포맷 지원
string josa3 = Josa.Choose("책", "이");        // "이"
string josa4 = Josa.Choose("연필", "이가");    // "가"
```

---

## 🚀 Caves of Qud 최적화 버전

### 버전 2: 게임 특화 최적화

```csharp
using System;
using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace CavesOfQud.KoreanJosa
{
    /// <summary>
    /// Caves of Qud 한글 조사 처리 시스템
    /// Josa.js 기반, 게임 환경에 최적화
    /// </summary>
    public static class JosaHandler
    {
        // 캐시 (성능 최적화)
        private static readonly Dictionary<string, string> Cache = 
            new Dictionary<string, string>(1000);
        
        // 조사 패턴 정규표현식
        private static readonly Regex JosaPattern = 
            new Regex(@"<([^>]+)><josa_(\w+)>", RegexOptions.Compiled);
        
        /// <summary>
        /// 텍스트 내 모든 조사 태그 처리
        /// </summary>
        public static string Process(string text)
        {
            if (string.IsNullOrEmpty(text))
                return text;
            
            // 캐시 확인
            if (Cache.TryGetValue(text, out string cached))
                return cached;
            
            // 조사 태그 처리
            string result = JosaPattern.Replace(text, match =>
            {
                string variable = match.Groups[1].Value;
                string josaType = match.Groups[2].Value;
                
                // 변수 값 가져오기 (게임 API 사용)
                string value = ResolveVariable(variable);
                
                // 조사 선택
                string josa = SelectJosa(value, josaType);
                
                return value + josa;
            });
            
            // 캐시 저장 (최대 1000개)
            if (Cache.Count < 1000)
                Cache[text] = result;
            
            return result;
        }
        
        /// <summary>
        /// 조사 선택 (Josa.js 알고리즘)
        /// </summary>
        private static string SelectJosa(string word, string type)
        {
            if (string.IsNullOrEmpty(word))
                return GetDefaultJosa(type);
            
            bool hasJong = HasJongseong(word);
            
            switch (type)
            {
                case "i_ga":
                case "subject":
                    return hasJong ? "이" : "가";
                
                case "eul_reul":
                case "object":
                    return hasJong ? "을" : "를";
                
                case "eun_neun":
                case "topic":
                    return hasJong ? "은" : "는";
                
                case "euro_ro":
                case "direction":
                    // ㄹ 받침 특수 처리
                    if (HasRieulJongseong(word))
                        return "로";
                    return hasJong ? "으로" : "로";
                
                case "a_ya":
                case "vocative":
                    return hasJong ? "아" : "야";
                
                case "wa_gwa":
                    return hasJong ? "과" : "와";
                
                default:
                    return "";
            }
        }
        
        /// <summary>
        /// 받침 확인 (Josa.js 알고리즘 + 개선)
        /// </summary>
        private static bool HasJongseong(string word)
        {
            if (string.IsNullOrEmpty(word))
                return false;
            
            // 특수문자 제거 후 마지막 한글 찾기
            char lastChar = GetLastKoreanChar(word);
            
            if (lastChar == '\0')
                return false;
            
            // 한글 범위 체크
            if (lastChar < 0xAC00 || lastChar > 0xD7A3)
                return false;
            
            // Josa.js 알고리즘
            return (lastChar - 0xAC00) % 28 > 0;
        }
        
        /// <summary>
        /// ㄹ 받침 확인
        /// </summary>
        private static bool HasRieulJongseong(string word)
        {
            if (string.IsNullOrEmpty(word))
                return false;
            
            char lastChar = GetLastKoreanChar(word);
            
            if (lastChar == '\0' || lastChar < 0xAC00 || lastChar > 0xD7A3)
                return false;
            
            return (lastChar - 0xAC00) % 28 == 8;
        }
        
        /// <summary>
        /// 마지막 한글 글자 추출 (특수문자 무시)
        /// </summary>
        private static char GetLastKoreanChar(string word)
        {
            for (int i = word.Length - 1; i >= 0; i--)
            {
                char c = word[i];
                if (c >= 0xAC00 && c <= 0xD7A3)
                    return c;
            }
            return '\0';
        }
        
        /// <summary>
        /// 변수 값 가져오기 (게임 API 연동)
        /// </summary>
        private static string ResolveVariable(string variable)
        {
            // TODO: 게임 API 연동
            // 예: player.name, item.name, entity.name 등
            return variable;
        }
        
        /// <summary>
        /// 기본 조사 (빈 문자열일 때)
        /// </summary>
        private static string GetDefaultJosa(string type)
        {
            switch (type)
            {
                case "i_ga":
                case "subject":
                    return "가";
                case "eul_reul":
                case "object":
                    return "를";
                case "eun_neun":
                case "topic":
                    return "는";
                case "euro_ro":
                case "direction":
                    return "로";
                case "a_ya":
                case "vocative":
                    return "야";
                case "wa_gwa":
                    return "와";
                default:
                    return "";
            }
        }
        
        /// <summary>
        /// 캐시 초기화 (메모리 관리)
        /// </summary>
        public static void ClearCache()
        {
            Cache.Clear();
        }
    }
}
```

---

## 📊 성능 비교

### Josa.js (원본)
- ✅ 매우 간결 (~60줄)
- ✅ 빠른 실행 속도
- ❌ 캐싱 없음
- ❌ 한글 범위 체크 없음
- ❌ ㄹ 받침 특수 케이스 없음

### 최적화 버전
- ✅ 캐싱 시스템 (1000개)
- ✅ 한글 범위 체크
- ✅ ㄹ 받침 특수 처리
- ✅ 특수문자 무시
- ✅ 게임 변수 연동
- ⚠️ 코드 길이 증가 (~200줄)

---

## 🧪 테스트 케이스

### Josa.js 검증 테스트

```csharp
[TestClass]
public class JosaHandlerTests
{
    [TestMethod]
    public void TestBasicJosa()
    {
        // Josa.js와 동일한 결과
        Assert.AreEqual("을", JosaHandler.SelectJosa("검", "eul_reul"));
        Assert.AreEqual("를", JosaHandler.SelectJosa("사과", "eul_reul"));
        Assert.AreEqual("이", JosaHandler.SelectJosa("책", "i_ga"));
        Assert.AreEqual("가", JosaHandler.SelectJosa("연필", "i_ga"));
    }
    
    [TestMethod]
    public void TestRieulJongseong()
    {
        // 개선 사항: ㄹ 받침
        Assert.AreEqual("로", JosaHandler.SelectJosa("서울", "euro_ro"));
        Assert.AreEqual("으로", JosaHandler.SelectJosa("집", "euro_ro"));
    }
    
    [TestMethod]
    public void TestSpecialCharacters()
    {
        // 개선 사항: 특수문자 무시
        Assert.AreEqual("이", JosaHandler.SelectJosa("책!", "i_ga"));
        Assert.AreEqual("을", JosaHandler.SelectJosa("검?", "eul_reul"));
    }
    
    [TestMethod]
    public void TestCaching()
    {
        // 성능 최적화: 캐싱
        string text = "당신은 <item.name><josa_eul_reul> 발견했다";
        
        string result1 = JosaHandler.Process(text);
        string result2 = JosaHandler.Process(text);
        
        // 두 번째 호출은 캐시에서 가져옴
        Assert.AreEqual(result1, result2);
    }
}
```

---

## 💡 핵심 개선 사항

### 1. 한글 범위 체크 추가
```csharp
// Josa.js: 체크 없음
return (string - 0xac00) % 28 > 0;

// 개선: 범위 체크
if (lastChar < 0xAC00 || lastChar > 0xD7A3)
    return false;
return (lastChar - 0xAC00) % 28 > 0;
```

### 2. ㄹ 받침 특수 처리
```csharp
// Josa.js: 없음
return _hasJong(string) ? '으로' : '로';

// 개선: ㄹ 받침 처리
if (HasRieulJongseong(word))
    return "로";
return hasJong ? "으로" : "로";
```

### 3. 캐싱 시스템
```csharp
// Josa.js: 없음

// 개선: 캐싱
private static readonly Dictionary<string, string> Cache = 
    new Dictionary<string, string>(1000);
```

### 4. 특수문자 처리
```csharp
// Josa.js: 마지막 글자만
string.charCodeAt(string.length - 1)

// 개선: 마지막 한글 글자 찾기
private static char GetLastKoreanChar(string word)
{
    for (int i = word.Length - 1; i >= 0; i--)
    {
        if (word[i] >= 0xAC00 && word[i] <= 0xD7A3)
            return word[i];
    }
    return '\0';
}
```

---

## 🎯 최종 권장 사항

### 사용할 코드
**버전 2: 게임 특화 최적화** (위 코드)

**이유:**
1. ✅ Josa.js의 검증된 알고리즘 사용
2. ✅ 게임 환경에 필요한 기능 추가
3. ✅ 성능 최적화 (캐싱)
4. ✅ 엣지 케이스 처리

### 다음 단계
1. 위 코드를 `JosaHandler.cs`로 저장
2. Harmony 패치 작성
3. 게임 내 테스트

---

**작성일:** 2026-01-13 09:58  
**기반 라이브러리:** Josa.js  
**최적화 수준:** ⭐⭐⭐⭐⭐
