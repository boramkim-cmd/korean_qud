# Caves of Qud 한글 조사 처리 시스템 - 스팀 모드 개발 전략

**작성일시:** 2026-01-13 09:51  
**폴더:** 2026-01-13-004  
**주제:** 한글 조사 자동 처리 시스템 (Steam Workshop Mod)

---

## 📋 목차

1. [문제 정의](#문제-정의)
2. [해결 방안 개요](#해결-방안-개요)
3. [스팀 모드 구조](#스팀-모드-구조)
4. [기술 스택](#기술-스택)
5. [구현 전략](#구현-전략)
6. [개발 로드맵](#개발-로드맵)
7. [테스트 전략](#테스트-전략)

---

## 문제 정의

### 핵심 문제

**영어:**
```
"You found <item.name>"
→ "You found sword"
→ "You found apple"
```

**한글 (문제 발생):**
```
"당신은 <item.name>을/를 발견했다"
→ "당신은 검을/를 발견했다" ❌
→ "당신은 사과을/를 발견했다" ❌
```

**올바른 결과:**
```
→ "당신은 검을 발견했다" ✅ (받침 O → 을)
→ "당신은 사과를 발견했다" ✅ (받침 X → 를)
```

### 조사 종류

| 조사 | 받침 O | 받침 X | 예시 |
|------|--------|--------|------|
| 주격 | 이 | 가 | 검**이**, 사과**가** |
| 목적격 | 을 | 를 | 검**을**, 사과**를** |
| 보조사 | 은 | 는 | 검**은**, 사과**는** |
| 방향 | 으로 | 로 | 집**으로**, 학교**로** |
| 호격 | 아 | 야 | 철수**야**, 영희**야** |

---

## 해결 방안 개요

### 방안 1: C# 모드 (⭐⭐⭐⭐⭐ 권장)

**장점:**
- 게임 엔진 레벨에서 처리
- 모든 텍스트에 자동 적용
- 성능 우수
- 한 번 설치하면 모든 번역에 적용

**단점:**
- C# 프로그래밍 필요
- 게임 API 이해 필요

### 방안 2: XML 전처리 스크립트 (⭐⭐⭐)

**장점:**
- 구현 간단 (Python)
- 번역 파일에만 적용
- 게임 수정 불필요

**단점:**
- 번역 파일마다 실행 필요
- 동적 생성 텍스트 처리 어려움

### 방안 3: 조사 회피 (⭐⭐)

**장점:**
- 기술적 구현 불필요
- 즉시 적용 가능

**단점:**
- 자연스럽지 않음
- 모든 문장 재작성 필요

---

## 스팀 모드 구조

### Caves of Qud 모드 시스템

```
Mods/
└── KoreanJosaHandler/
    ├── manifest.json           (모드 메타데이터)
    ├── Scripts/
    │   └── JosaHandler.cs      (C# 조사 처리 로직)
    ├── Assemblies/
    │   └── KoreanJosaHandler.dll (컴파일된 DLL)
    └── README.md               (사용 설명서)
```

### manifest.json 예시

```json
{
  "id": "KoreanJosaHandler",
  "title": "Korean Particle Handler (한글 조사 자동 처리)",
  "author": "YourName",
  "version": "1.0.0",
  "description": "Automatically handles Korean particles (이/가, 을/를, 은/는) for dynamic text generation.",
  "tags": ["Localization", "Korean", "Utility"],
  "preloadScripts": [
    "Scripts/JosaHandler.cs"
  ]
}
```

---

## 기술 스택

### 1. C# (게임 엔진 언어)

**Caves of Qud는 Unity 기반**
- C# 스크립팅 지원
- Harmony 패치 라이브러리 사용 가능

### 2. 필요한 지식

**기본:**
- C# 프로그래밍
- 유니코드 한글 처리
- 정규표현식

**고급:**
- Unity 모딩
- Harmony 패치
- Caves of Qud API

---

## 구현 전략

### Phase 1: 받침 감지 알고리즘

```csharp
public class KoreanJosaHandler
{
    // 받침 확인 함수
    public static bool HasJongseong(string word)
    {
        if (string.IsNullOrEmpty(word))
            return false;
        
        char lastChar = word[word.Length - 1];
        
        // 한글 유니코드 범위: AC00-D7A3
        if (lastChar < 0xAC00 || lastChar > 0xD7A3)
            return false; // 한글이 아님
        
        // 종성(받침) 계산
        int jongseong = (lastChar - 0xAC00) % 28;
        
        return jongseong != 0; // 0이면 받침 없음
    }
}
```

### Phase 2: 조사 선택 함수

```csharp
public static string GetJosa(string word, JosaType type)
{
    bool hasJongseong = HasJongseong(word);
    
    switch (type)
    {
        case JosaType.Subject: // 이/가
            return hasJongseong ? "이" : "가";
        
        case JosaType.Object: // 을/를
            return hasJongseong ? "을" : "를";
        
        case JosaType.Topic: // 은/는
            return hasJongseong ? "은" : "는";
        
        case JosaType.Direction: // 으로/로
            return hasJongseong ? "으로" : "로";
        
        case JosaType.Vocative: // 아/야
            return hasJongseong ? "아" : "야";
        
        default:
            return "";
    }
}

public enum JosaType
{
    Subject,    // 이/가
    Object,     // 을/를
    Topic,      // 은/는
    Direction,  // 으로/로
    Vocative    // 아/야
}
```

### Phase 3: 텍스트 처리 함수

```csharp
public static string ProcessText(string text)
{
    // 패턴: <변수><josa_type>
    // 예: "<item.name><josa_eul_reul>"
    
    string pattern = @"<([^>]+)><josa_(\w+)>";
    
    return Regex.Replace(text, pattern, match =>
    {
        string variable = match.Groups[1].Value;
        string josaTypeStr = match.Groups[2].Value;
        
        // 변수 값 가져오기 (게임 API 사용)
        string value = GetVariableValue(variable);
        
        // 조사 타입 결정
        JosaType josaType = ParseJosaType(josaTypeStr);
        
        // 조사 선택
        string josa = GetJosa(value, josaType);
        
        // 결과 반환
        return value + josa;
    });
}
```

### Phase 4: 게임 텍스트 시스템 후킹

```csharp
using HarmonyLib;

[HarmonyPatch(typeof(ConversationNode), "GetText")]
public class ConversationTextPatch
{
    static void Postfix(ref string __result)
    {
        // 대화 텍스트 처리
        __result = KoreanJosaHandler.ProcessText(__result);
    }
}

[HarmonyPatch(typeof(Quest), "GetDescription")]
public class QuestTextPatch
{
    static void Postfix(ref string __result)
    {
        // 퀘스트 텍스트 처리
        __result = KoreanJosaHandler.ProcessText(__result);
    }
}
```

---

## 개발 로드맵

### Week 1: 환경 설정 및 기초 구현

**Day 1-2: 개발 환경 설정**
```
- [ ] Visual Studio 설치
- [ ] Caves of Qud 모딩 SDK 설치
- [ ] Harmony 라이브러리 설치
- [ ] 테스트 모드 프로젝트 생성
```

**Day 3-4: 받침 감지 알고리즘**
```
- [ ] HasJongseong() 함수 구현
- [ ] 단위 테스트 작성
- [ ] 엣지 케이스 처리 (숫자, 영어 등)
```

**Day 5-7: 조사 선택 로직**
```
- [ ] GetJosa() 함수 구현
- [ ] 5가지 조사 타입 지원
- [ ] 테스트 케이스 작성
```

### Week 2: 게임 통합

**Day 8-10: 텍스트 처리 시스템**
```
- [ ] ProcessText() 함수 구현
- [ ] 정규표현식 패턴 최적화
- [ ] 변수 값 가져오기 구현
```

**Day 11-12: Harmony 패치**
```
- [ ] ConversationNode 패치
- [ ] Quest 패치
- [ ] Item 설명 패치
```

**Day 13-14: 테스트 및 디버깅**
```
- [ ] 게임 내 테스트
- [ ] 버그 수정
- [ ] 성능 최적화
```

### Week 3: 배포 및 문서화

**Day 15-17: 스팀 워크샵 준비**
```
- [ ] manifest.json 작성
- [ ] README.md 작성 (한글/영어)
- [ ] 스크린샷 준비
- [ ] 사용 예시 작성
```

**Day 18-19: 스팀 워크샵 업로드**
```
- [ ] 모드 빌드
- [ ] 스팀 워크샵 등록
- [ ] 설명 페이지 작성
```

**Day 20-21: 커뮤니티 피드백**
```
- [ ] 버그 리포트 대응
- [ ] 기능 개선
- [ ] 패치 배포
```

---

## 사용 예시

### 번역 파일에서 사용

**기존 방식 (문제):**
```xml
<text>당신은 <item.name>을/를 발견했다</text>
```

**모드 적용 후:**
```xml
<text>당신은 <item.name><josa_eul_reul> 발견했다</text>
```

**게임 내 결과:**
```
item.name = "검"
→ "당신은 검을 발견했다" ✅

item.name = "사과"
→ "당신은 사과를 발견했다" ✅
```

### 지원하는 태그

```xml
<josa_i_ga>      <!-- 이/가 -->
<josa_eul_reul>  <!-- 을/를 -->
<josa_eun_neun>  <!-- 은/는 -->
<josa_euro_ro>   <!-- 으로/로 -->
<josa_a_ya>      <!-- 아/야 -->
```

---

## 테스트 전략

### Unit Test (단위 테스트)

```csharp
[TestClass]
public class JosaHandlerTests
{
    [TestMethod]
    public void TestHasJongseong()
    {
        Assert.IsTrue(HasJongseong("검"));    // 받침 O
        Assert.IsFalse(HasJongseong("사과")); // 받침 X
        Assert.IsFalse(HasJongseong("ABC"));  // 한글 아님
    }
    
    [TestMethod]
    public void TestGetJosa()
    {
        Assert.AreEqual("을", GetJosa("검", JosaType.Object));
        Assert.AreEqual("를", GetJosa("사과", JosaType.Object));
        Assert.AreEqual("이", GetJosa("책", JosaType.Subject));
        Assert.AreEqual("가", GetJosa("연필", JosaType.Subject));
    }
    
    [TestMethod]
    public void TestProcessText()
    {
        string input = "당신은 <item.name><josa_eul_reul> 발견했다";
        
        // item.name = "검"
        string result1 = ProcessText(input, new { item = new { name = "검" } });
        Assert.AreEqual("당신은 검을 발견했다", result1);
        
        // item.name = "사과"
        string result2 = ProcessText(input, new { item = new { name = "사과" } });
        Assert.AreEqual("당신은 사과를 발견했다", result2);
    }
}
```

### Integration Test (통합 테스트)

```
1. 게임 실행
2. 새 게임 시작
3. 아이템 획득
   → 메시지 확인: "검을 발견했다" ✅
4. NPC 대화
   → 조사 확인: "바라스럼이 말했다" ✅
5. 퀘스트 확인
   → 조사 확인: "유물을 찾아라" ✅
```

### Edge Case (엣지 케이스)

```csharp
// 숫자로 끝나는 경우
"레벨 5" → "레벨 5가" (받침 없음으로 처리)

// 영어로 끝나는 경우
"Sword" → "Sword가" (받침 없음으로 처리)

// 특수문자로 끝나는 경우
"검!" → "검이" (특수문자 제외하고 판단)

// 빈 문자열
"" → "가" (기본값)
```

---

## 고급 기능

### 1. 스마트 변수 처리

```csharp
// HistorySpice.json의 변수 처리
"<spice.adjectives.!random><josa_eun_neun> 프리즘"

// 런타임에 adjectives 값 가져와서 조사 결정
"빛나는" + "은" → "빛나는은" ❌
→ 형용사는 조사 불필요, 스킵 처리
```

### 2. 문맥 인식

```csharp
// "으로/로" 특수 케이스
"집" + "으로" → "집으로" ✅
"학교" + "로" → "학교로" ✅

// 하지만 "ㄹ" 받침은 예외
"서울" + "로" → "서울로" ✅ (으로 아님!)
```

### 3. 캐싱 시스템

```csharp
// 성능 최적화: 이미 처리한 텍스트 캐싱
private static Dictionary<string, string> cache = new Dictionary<string, string>();

public static string ProcessTextCached(string text)
{
    if (cache.ContainsKey(text))
        return cache[text];
    
    string result = ProcessText(text);
    cache[text] = result;
    return result;
}
```

---

## 배포 체크리스트

### 스팀 워크샵 업로드 전

```markdown
- [ ] 모든 테스트 통과
- [ ] 버그 없음 확인
- [ ] 성능 문제 없음
- [ ] manifest.json 완성
- [ ] README.md 작성 (한글/영어)
- [ ] 스크린샷 준비 (3-5장)
- [ ] 사용 예시 동영상 (선택)
- [ ] 라이선스 명시
```

### README.md 구조

```markdown
# Korean Particle Handler (한글 조사 자동 처리)

## 기능
- 한글 조사 자동 처리 (이/가, 을/를, 은/는, 으로/로, 아/야)
- 모든 동적 텍스트 지원
- 성능 최적화

## 설치 방법
1. 스팀 워크샵 구독
2. 게임 재시작
3. 모드 활성화

## 사용 방법
번역 파일에서 `<josa_type>` 태그 사용

## 호환성
- Caves of Qud 버전: 2.0.x 이상
- 다른 한글화 모드와 호환

## 라이선스
MIT License
```

---

## 대안: Python 전처리 스크립트

**모드 개발이 어려운 경우:**

```python
import re
import json

def has_jongseong(word):
    if not word:
        return False
    last_char = word[-1]
    if not ('가' <= last_char <= '힣'):
        return False
    return (ord(last_char) - 0xAC00) % 28 != 0

def get_josa(word, josa_type):
    has_jong = has_jongseong(word)
    
    josa_map = {
        'i_ga': ('이', '가'),
        'eul_reul': ('을', '를'),
        'eun_neun': ('은', '는'),
        'euro_ro': ('으로', '로'),
        'a_ya': ('아', '야')
    }
    
    return josa_map[josa_type][0 if has_jong else 1]

def process_xml(xml_content, variables):
    """
    XML 파일 전처리
    variables: 게임에서 사용되는 변수 값들
    """
    pattern = r'<([^>]+)><josa_(\w+)>'
    
    def replacer(match):
        var_name = match.group(1)
        josa_type = match.group(2)
        
        # 변수 값 가져오기
        value = variables.get(var_name, var_name)
        
        # 조사 선택
        josa = get_josa(value, josa_type)
        
        return value + josa
    
    return re.sub(pattern, replacer, xml_content)

# 사용 예시
xml = '<text>당신은 <item.name><josa_eul_reul> 발견했다</text>'
variables = {'item.name': '검'}
result = process_xml(xml, variables)
print(result)  # "당신은 검을 발견했다"
```

---

## 참고 자료

### Caves of Qud 모딩

- **공식 모딩 가이드:** https://wiki.cavesofqud.com/wiki/Modding
- **Steam Workshop:** https://steamcommunity.com/app/333640/workshop/
- **Discord 커뮤니티:** Caves of Qud 공식 디스코드

### C# 한글 처리

- **유니코드 한글:** U+AC00 ~ U+D7A3
- **초성, 중성, 종성 분리 알고리즘**
- **Harmony 패치 라이브러리**

---

## 다음 단계

1. **문서 01 읽기:** C# 구현 상세 가이드
2. **문서 02 읽기:** Python 전처리 스크립트
3. **문서 03 읽기:** 스팀 워크샵 배포 가이드

---

**작성일:** 2026-01-13 09:51  
**버전:** 1.0  
**다음 문서:** 01_C#_구현_가이드.md
