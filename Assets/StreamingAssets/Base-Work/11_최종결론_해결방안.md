# 최종 결론 및 해결 방안

**작성일시:** 2026-01-13 11:15  
**상태:** Harmony 패치 실패, 대안 필요

---

## 🔍 발견한 문제

### Harmony 패치 에러
```
MODERROR - Exception applying harmony patches:
Undefined target method for patch method Quest_Patch::GetDisplayText_Postfix
```

**원인:** `XRL.World.Quest.GetDisplayText()` 메서드가 존재하지 않음

---

## 💡 해결 방안

### 방안 1: Python 전처리 스크립트 (권장) ⭐⭐⭐⭐⭐

XML 파일을 게임에 넣기 전에 미리 처리:

```python
# preprocess_quests.py
import re

def replace_josa(text):
    # "단어(조사1)조사2" → "단어조사"
    pattern = r'(\S+)\(([^)]+)\)([^(\s]*)'
    
    def replacer(match):
        word = match.group(1)
        josa1 = match.group(2)
        josa2 = match.group(3)
        
        has_jong = has_jongseong(word)
        selected = josa1 if has_jong else josa2
        
        return word + selected
    
    return re.sub(pattern, replacer, text)

# XML 파일 처리
with open('Quests_source.xml', 'r') as f:
    content = f.read()

processed = replace_josa(content)

with open('Quests.xml', 'w') as f:
    f.write(processed)
```

**장점:**
- ✅ 즉시 작동
- ✅ Harmony 불필요
- ✅ 100% 안정적

### 방안 2: 수동 번역

조사를 미리 결정해서 XML에 넣기:

```xml
<!-- 원본 -->
<text>붉은 바위(으)로 가라</text>

<!-- 수동 처리 -->
<text>붉은 바위로 가라</text>
```

### 방안 3: 다른 Harmony 타겟 찾기

XML 로드 시점을 패치:

```csharp
[HarmonyPatch(typeof(XmlDataHelper), "LoadQuests")]
```

---

## 🎯 권장 사항

**Python 전처리 스크립트를 사용하세요.**

1. `Translation/` 폴더에서 작업
   - `Quests_source.xml` (조사 태그 포함)
   
2. Python 스크립트로 처리
   - `python preprocess.py`
   
3. 결과를 `Mod/` 폴더로 복사
   - `Quests.xml` (조사 처리 완료)

---

**작성일:** 2026-01-13 11:15
